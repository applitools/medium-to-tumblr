'use strict'
const {describe, it} = require('mocha')
const {expect} = require('chai')

describe('medium-to-tumblr', function() {
  it('should do amazing stuff', async () => {
    expect(4).to.equal(4)
  })
})
